<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.LoginMapper">

    <select id="Login" resultType="com.example.demo.model.LoginModel" >
   		SELECT email,password,role,deleteflg
	FROM visalogintable
	WHERE email = #{email} AND password = #{password} AND deleteflg='ACTIVE'
	
    </select>

</mapper>

